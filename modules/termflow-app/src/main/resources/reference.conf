termflow {
  logging {
    path = ${user.home}"/.termflow/termflow.log"
    path = ${?TERMFLOW_LOG_PATH}
  }
  metrics {
    enabled = false
    env-override = ${?TERMFLOW_RENDER_METRICS}
  }
  accessibility {
    reduced-motion = false
    env-override = ${?TERMFLOW_REDUCED_MOTION}
  }
}
